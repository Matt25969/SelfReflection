pipeline{
        agent any
        stages{ 
                stage('---clean---'){
                        steps{
                              sh "sudo docker-compose up --build"
                        }
                }
        }
}
